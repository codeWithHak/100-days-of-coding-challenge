let userName:string = "huzair";

console.log(userName.toLowerCase()); // This will convert username to lower case (small)

console.log(userName.toUpperCase()); // This will convert the username to upper case (capital)

console.log(userName[0].toUpperCase() + userName.slice(1).toLowerCase()); // This will convert the first letter to capital and the rest to small.


export{} // this exoprt is just to prevent red squiggly lines



